const { expect } = require('chai');
const reducer = require('../../frontend/modules/user/evaluation');

describe('Evaluation reducer', () => {
  it('returns the initial value', () => {
    expect(true).to.equal(true);
  })
});
