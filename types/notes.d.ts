type UnhydratedNote = {
  note: string,
  userId: string,
  skillId: string
  createdDate: string,
  deleted: boolean,
};

type NoteViewModel = {
  note: string,
  userId: string,
  skillId: string
  createdDate: string,
};
