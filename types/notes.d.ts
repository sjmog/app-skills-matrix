type UnhydratedNote = {
  note: string,
  userId: string,
  skillId: string
  createdDate: string,
  deleted: boolean,
};

type NoteViewModel = {
  id: string,
  note: string,
  userId: string,
  skillId: string
  createdDate: string,
};
